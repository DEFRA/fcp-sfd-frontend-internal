{% extends 'common/layout.njk' %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block beforeContent %}
  <div class="govuk-grid-row sub-header-container govuk-!-padding-top-3">
    <div class="govuk-grid-column-full govuk-!-text-align-right">
      <a href="/auth/sign-out" class="govuk-link">Sign out</a>
    </div>
  </div>
{% endblock %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">
        <span class="govuk-caption-l">{{ auth.name }}</span>
        You have signed in
      </h1>

      <h2 class="govuk-heading-m">DAL Data</h2>
      <dl class="govuk-summary-list">
        <div class="govuk-summary-list__row">
          <dt class="govuk-summary-list__key">SBI</dt>
          <dd class="govuk-summary-list__value">{{ dalData.business.sbi }}</dd>
        </div>
        <div class="govuk-summary-list__row">
          <dt class="govuk-summary-list__key">Organisation ID</dt>
          <dd class="govuk-summary-list__value">{{ dalData.business.organisationId }}</dd>
        </div>
        <div class="govuk-summary-list__row">
          <dt class="govuk-summary-list__key">CRN</dt>
          <dd class="govuk-summary-list__value">{{ dalData.business.customer.crn }}</dd>
        </div>
        <div class="govuk-summary-list__row">
          <dt class="govuk-summary-list__key">First Name</dt>
          <dd class="govuk-summary-list__value">{{ dalData.business.customer.firstName }}</dd>
        </div>
        <div class="govuk-summary-list__row">
          <dt class="govuk-summary-list__key">Last Name</dt>
          <dd class="govuk-summary-list__value">{{ dalData.business.customer.lastName }}</dd>
        </div>
        <div class="govuk-summary-list__row">
          <dt class="govuk-summary-list__key">Role</dt>
          <dd class="govuk-summary-list__value">{{ dalData.business.customer.role }}</dd>
        </div>
      </dl>

      <h3 class="govuk-heading-m"> Business details</h3>
      <p>
        <a href="/business-details">View and update your business details</a>
      </p>
      <h3 class="govuk-heading-m"> Personal details</h3>
      <p>
        <a href="/personal-details">View and update your personal details</a>
      </p>

      <span class="govuk-caption-m">{{ auth.organisationId }}</span>
      <h2 class="govuk-heading-m">Your session details</h2>

      <dl class="govuk-summary-list">
        {% for key, value in auth %}
          <div class="govuk-summary-list__row">
            <dt class="govuk-summary-list__key">{{ key }}</dt>
            <dd class="govuk-summary-list__value">{{ value }}</dd>
          </div>
        {% endfor %}
      </dl>

    </div>
  </div>
{% endblock %}
